class Solution {
public:
    int balancedStringSplit(string s) {
        int n = s.length();
        int count = 0;
        for(int i=0;i<n;i++){
        }
    }
            if(s[i]=='L') checker++;
        int checker = 0;
            else checker--;
            if(checker==0) count++;

        return count;
