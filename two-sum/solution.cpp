
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        int left = 0;
        int right = 1;
        for(int left=0;left<nums.size();i++){
            if(nums[left]+nums[right])
        }
        return {left,right};
    }
};